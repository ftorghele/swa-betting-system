var services = require('./services.js')
;

function analyzeBet(userid, data, callback) {
    console.log("analyzeBet: userid: " + userid + " ------ data: " + data + " ------- callback: " + callback);
}